<nav class="navbar navbar-expand-lg navbar-light mb-3" style="background-color: #f1f1f1;">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link but-orange" href="{{ path('index') }}">Главная</a>
            </li>
        </ul>
        <ul class="nav justify-content-end">
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') != true %}
                <li class="nav-item">
                    <a class="nav-link but-green" href="{{ path('seller_requests_new')}}">Стать продавцом</a>
                </li>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') and (is_granted('ROLE_ADMIN') != true) %}
                <li class="nav-item">
                    <a class="nav-link but-green" href="{{ path('profile') }}">Аккаунт</a>
                </li>
            {% endif %}
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') != true %}
                <li class="nav-item">
                    <a class="nav-link but-green" href="{{ path('app_register') }}">Регистрация</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link but-green" href="{{ path('app_login') }}">Войти</a>
                </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link but-green" href="{{ path('admin_dashboard') }}">Панель администратора</a>
                </li>
            {% endif %}
        </ul>

    </div>
</nav>