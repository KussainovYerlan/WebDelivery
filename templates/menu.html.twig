<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #f1f1f1;">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link but-orange" href="{{ path('index') }}">Главная</a>
            </li>
        </ul>
        <ul class="nav justify-content-end">
            <li class="nav-item">
                <a class="nav-link but-green" href="{{ path('product_index')}}">Продукты</a>
            </li>
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <li class="nav-item">
                <a class="nav-link but-green" href="#">Аккаунт</a>
            </li>
            <li class="nav-item">
                <a class="nav-link but-green" href="{{ path('logout') }}">Выйти</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a class="nav-link but-green" href="{{ path('app_register') }}">Регистрация</a>
            </li>
            <li class="nav-item">
                <a class="nav-link but-green" href="{{ path('app_login') }}">Войти</a>
            </li>
        {% endif %}
            <li class="nav-item">
                <a class="btn btn-primary" id="shopping-cart_open" href="#">Корзина</a>
            </li>
        </ul>

    </div>
</nav>