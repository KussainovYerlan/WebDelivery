<form method="post" action="{{ path('delivery_order_delete', {'id': delivery_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery_order.id) }}">
    <button class="btn">Delete</button>
</form>
